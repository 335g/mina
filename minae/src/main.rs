//! minae: TUI（1からの再構築予定。現在は空の bin クレート）。
//!
//! daemon 操作は `minad`、agent CLI は `minas` を使う。

fn main() {
    eprintln!("minae TUI は再構築中です。daemon 操作は minad、agent CLI は minas を使ってください。");
    std::process::exit(1);
}
