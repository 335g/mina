[
  {
    "name": "usage",
    "description": "when to use minas instead of shell tools (rg/sed), and the rules that hold from the first command",
    "body": "USAGE — when to use minas, and the rules that hold from the first command\n\nUse minas instead of shell text tools (rg / sed / cat / awk) for source work:\n  orient     minas outline <path> [--recursive] | minas symbol <path> <name>\n             minas search <path> <pat> | minas at <path> <line>:<col> | minas hover <path> <line>:<col>\n  read       minas read <path> [--lines <a>:<b>]        any file, buffer-free\n  edit       minas apply <path> <old> <new>             content-addressed; --hunks-stdin for batches\n  refactor   minas rename <path> <old> <new>            + minas references to verify (identifier-only)\n  verify     minas check <path>...                      LSP diagnostics — the real build is the gate\n  delete     minas delete <path>                        moving = read | apply --whole-stdin, then delete\n\nThe command surface lives in this shelf, not in your prompt. Ask for a topic before concluding that\na command is broken (an empty answer can be an unverified one, not a missing feature):\n  minas skill            index: one line per topic, ~15 lines\n  minas skill <topic>    the contract for that topic\n\nThree rules hold from the first command:\n  1. Never compute positions. `minas apply` (old text -> new text) is the editing entrance; the\n     daemon resolves the position and rejects a stale match. `minas edit --path <f>` needs `expected_text`\n     and silently corrupts a shifted range without it; `--path` is REQUIRED and checked against\n     the daemon's current document before the edit is sent (a path-less positional edit lands in\n     whatever document the daemon has focused — another pane can change that between your `get`\n     and your `edit`, 2026-09-15).\n  2. `minas check` reports LSP diagnostics, not a build. Exit 0 is not \"verified clean\" (an empty\n     check is settled:false) — run the real build / test to conclude.\n  3. If you fall back to a shell tool, say so and why. A silent fallback hides a tool gap; it is a\n     finding to report, not a workaround to bury.\n\nEdits are verified by content: `apply` refuses when the old text no longer matches, and names what\nchanged. Re-read the affected range and retry instead of retrying blindly — `minas skill errors`\nis the first stop on a rejection.\n\nLSP-FREE VS LSP-BACKED: `read`, `search`, `edit` (apply / delete / whole-file), `wait`, `get`,\n`errors`, `exec` work without any language server (persisting is `minas exec '\"Save\"'` — `persist` is a skill TOPIC, not a command). `outline`, `at`, `hover`, `symbol`,\n`references`, `rename`, `check`, `hints`, `peek` need one: a language with no server configured\n(e.g. Python) gets an explicit `not supported …` refusal, exit 1 — never an empty success. The\n`minas skill` index marks those topics with `*`.\n"
  },
  {
    "name": "read",
    "description": "map files with outline; read only the lines you need",
    "body": "READ — reading files without wasting tokens\n\nUse:      minas read <path> [--lines <start>:<end>]   (any file, buffer-free — ADR-0048)\n          minas read <path> --span <anchor>:<head>    (CHAR OFFSETS, 0-origin — the units\n                                                      `at`/`outline`/`hints` return)\n          minas get --lines <start>:<end>            (current buffer only)\nOutput:   numbered lines: {\"n\":1500,\"text\":\"...\"}  (1-origin, end may be empty = last line)\n\nRules\n- REVISION: every positional answer carries the content revision it was computed\n  from — the WHOLE FILE's fnv1a64. `read --lines` / `get --lines` / `search` / `at` /\n  `hover` print it as a `\"checksum\"` JSON field; plain `minas read <file>` (raw text on\n  stdout, still pipeable) and `minas outline <file>` print `read: revision <n>` /\n  `outline: revision <n>` on stderr; `get` without `--lines` carries `checksum` in the\n  snapshot. Read it as \"did this FILE change between my two calls\": two ranges of the\n  same file share the value, so it does NOT certify that a range you hold is still\n  valid inside an unchanged file (nothing can — re-read and compare the text). What it\n  does certify: if the number differs, every range/line from the earlier call is stale.\n  ADR-0063. `symbol` and `--recursive` outline have no single revision on purpose\n  (they span files) — resolve each node's `path` and compare that file's own value.\n- NO DIRECTORY LISTING: minas reads files, not trees — there is no list/find/glob\n  command. Enumerate with the gitignore-aware platform tools\n  (`git ls-files '*.rs'`, `rg --files`) and then use minas for content and structure\n  (`outline`, `at`, `symbol`, `references`, `check`). A hand-rolled walker would\n  include gitignored scratch trees (measured: 2480 of 2520 `.rs` files in this repo\n  are under a gitignored `tmp/`).\n- SIZE CAP (same thing, but for the *reason*): `cannot open <path>: not a file, is a\n  directory` vs a missing file's `No such file or directory (os error 2)` — the\n  message names which one it was, so a directory is no longer confusable with a typo.\n- SIZE CAP: any file above 16 MiB (MAX_FILE_SIZE) is refused outright —\n  `read: file too large: <path>`, exit 1. The range does NOT help: the daemon\n  reads the file to serve `--lines`, so `--lines 1:1` on a 24 MB file fails the\n  same way (dogfooding #11). For big logs/artifacts, narrow the file with the\n  shell (head/tail/split) instead of expecting minas to stream it.\n- `--span <anchor>:<head>` reads by CHAR OFFSET (0-origin, `head` exclusive, may be empty\n  = end of file) — the SAME units `at` / `outline` / `hints` put in their\n  `range` / `selection_range` / `span` fields, so `outline` -> `read --span` needs no\n  conversion. `--lines` is 1-origin LINE NUMBERS and the two are NOT interchangeable: fed\n  an offset, `--lines` returns `lines: []` with a `note` and exit 0 (measured 2026-09-15:\n  `selection_range` 346:350 of a name token against a 53-line file). Use `--lines` for\n  reading around a `search`/`hover` line, `--span` for a symbol whose range the LSP gave\n  you. Same JSON shape (`char_count` instead of `line_count`) and the same out-of-range\n  contract (explained empty, exit 0). The two flags conflict by design.\n- Read only the lines you need (measured: ranged read cuts output by 82.6% at\n  the median over a real repo, n=18; the larger the file the bigger the win).\n  CAUTION: `--lines` adds a JSON envelope (~0.7KB fixed cost: path, generation,\n  line_count, per-line numbers), so on a small file (<~1KB) plain `minas read`\n  can be CHEAPER than `--lines` (measured: 392B whole vs 733B ranged; rust2\n  #32). Skip --lines for tiny files.\n- `minas read <path>` reads ANY file without touching the current buffer — no\n  Open/apply needed just to look (a plain `minas read <path>` prints the raw\n  text; `--lines` prints numbered lines). `minas get` reads only the buffer\n  (last Open/apply) and returns the full snapshot; add `--brief` to drop the\n  history fields (events/activity) when you only need text/checksum/dirty\n  (rust2 #21: 78-char buffer 79KB -> ~830B). Same `--brief` works on `wait`\n  and `edit` responses.\n- On an unfamiliar file, minas outline FIRST (structure + exact spans for a\n  fraction of the bytes), then read only the symbol you need; minas at\n  <path> <line>:<col> names the symbol enclosing a spot you are about to touch,\n  so both reads and edits happen at symbol granularity.\n- For types, minas hover <path> <line>:<col> returns the signature without\n  any read; to find where a name lives in the workspace, minas symbol\n  <path> <query> replaces rg (both ADR-0032).\n- The numbered output lets you paste the exact text you saw into `minas apply`\n  as the <old> argument — read and edit share one contract.\n- Out-of-range start returns an explained zero result, e.g.\n  \"no lines in 3000..3100: file has 2000 lines\" — this is not an error, just pick\n  a valid range.\n- A whole-file read is allowed only when you really need the full text; prefer\n  --lines otherwise. A whole-file `minas get` is allowed only when you need the\n  checksum or diagnostic machinery of the snapshot.\n- ACTIVITY ENTRIES: `activities[].kind` is the machine-readable field; `label` is\n  TUI-facing and localised (e.g. 診断取得中). Match on `kind`, never on `label`\n  (dogfooding self-host #7).\n- `diagnostics_unavailable: true` in the snapshot printed by `minas get` (the full StateSnapshot;\n  `minas info` does NOT carry it — it lists servers/roots/sessions only) means the server cannot be\n  asked for diagnostics at\n  all (push-only, e.g. typescript-language-server), so an empty `diagnostics` is NOT \"no problems\"\n  — use the project's own gate (`tsc --noEmit` / `vitest`). It stays true across reads (unlike the\n  one-shot `status` line), and `minas check` refuses the same way with `not supported`\n  (ADR-0066). It is ALSO true when the path has NO language entry at all (Python: dogfooding\n  #8) — in that case `diagnostics: []`, `peek: null` and `inlay_hints: []` in the same snapshot\n  are \"nothing to ask\", not answers; read the marker, not the empty fields."
  },
  {
    "name": "search",
    "description": "minas search <path> <query> (or --crate-root <entry.rs> <query>): literal match positions without reading the file",
    "body": "SEARCH — locate occurrences without reading the file\n\nUse:      minas search <path> <query> [-i|--ignore-case] [-w|--word]\n          minas search --crate-root <entry.rs> <query> [--include-tests]\n                                    (whole crate in ONE call — the sweep\n                                     `rename`/`references` tell you to run)\n\nOutput:   compact JSON: {\"path\",\"generation\",\"total\",\"truncated\",\n          \"matches\":[{\"line\",\"col\",\"len\"}]} — 1-origin line, 1-origin CHAR\n          column, char length. No line text is returned: read only the lines\n          you need with `minas read <path> --lines <line>:<line>`.\n\nThe intended loop (locate cheaply, read narrowly):\n    minas search src/x.rs \"TODO\"            # -> matches: [{line:12,col:5,len:4}, ...]\n    minas read src/x.rs --lines 12:12       # -> the line text at that address\n- Buffer-first: the open document's unsaved edits are searched, else the file\n- SIZE CAP: files above 16 MiB are refused (`search: file too large: <path>`,\n  exit 1) — same cap as read; there is no ranged form of search.\n\n  on disk (same resolution as `read`).\n- Case-sensitive by default; `-i` searches case-insensitively.\n- At most 200 matches are returned; `total` is the full count and `truncated`\n  says whether the list was cut (rust2 ask: agents could not get match\n  positions without reading the whole file).\n- `--crate-root <entry.rs> <query>` (2026-09-15): search a WHOLE crate in one call —\n  same expansion as `check --crate-root` (the entry file, then every `.rs` under its\n  directory, sorted; `--include-tests` adds the sibling `tests/`). `-w`/`-i` apply.\n  Output is `{\"searched\":N,\"total\":M,\"files\":[{\"path\",\"generation\",\"checksum\",\n  \"total\",\"truncated\",\"matches\":[…]}]}` — files with zero matches are omitted, so\n  `total` is the crate-wide count. A file that fails is named on stderr and the command\n  exits 1 (the other files are still searched and printed). Use it for the rename sweep:\n  `rename`/`references` say \"verify with `minas search -w`\", and without this flag that\n  sweep is one call PER FILE (measured: 26 files for one symbol).\n- Matches are PLAIN SUBSTRINGS by default (no word boundaries, no regex).\n  `-w`/`--word` restricts hits to word boundaries (ASCII identifiers), which is\n  the direct fix for the rename-sweep trap: `TOTAL_COUNT` no longer matches\n  `TOTAL_COUNT_HEADER` (rust2 #44/#49). Non-ASCII characters count as\n  boundaries, so a Japanese comment next to the match does not hide it.\n- Use it to find where something lives when `symbol` (definitions) is not the\n  question — e.g. strings, config keys, comments, or a text that is not an\n  identifier. For definitions prefer `minas symbol`; for references to a symbol\n  prefer `minas references` (LSP-accurate)."
  },
  {
    "name": "wait",
    "description": "minas wait <generation>: block until the state moves (settled), then return the snapshot",
    "body": "WAIT — block until the document moves, without polling\n\nUse:      minas wait <generation>            (blocking; returns the snapshot)\n          minas wait --brief <generation>    (same, minus events/activity — ~1KB)\n\nRules\n- Pass a generation you already saw (e.g. from `minas apply`'s result or\n  `minas get --brief`). It blocks until the daemon's generation exceeds it,\n  then returns that snapshot. No polling, no idle traffic (ADR-0014).\n- Content changes are SETTLED before the wait returns (rust2 #16/#18): an edit\n  batch (apply = Open -> replace_range -> Save) wakes you only after the text\n  stops changing (~120ms quiet), so you get the settled text, not the pre-edit\n  text at the Open stage. Several hunks (or several quick edits) are swallowed\n  into one wake with the final state.\n- Already-quiet returns immediately: if the last content change is older than\n  the quiet window, `wait <old-generation>` answers in ~10ms — usable as a\n  cheap \"has anything moved?\" check.\n- If no content change ever arrives it falls back after ~500ms and returns the\n  current snapshot (ADR-0014's immediate-return contract is preserved); the CLI\n  bounds the whole wait at 90s and exits 2 (retryable) on timeout.\n- Open/Save/mode changes alone do not wake it — only content-changing events\n  (edit, Undo/Redo, external change, rename). If you need to observe a focus\n  change, use `get` instead.\n- A daemon restart during a wait ends it with `wait: the daemon closed the connection …\n  (retryable)` and exit 2 — a lost connection, not a bad generation: re-run the wait, or read the\n  current state with `minas get --brief` (measured 2026-09-15; before this it surfaced as a raw\n  `invalid response: EOF while parsing a value`, exit 1).\n- In a wait loop, always use --brief: the full snapshot per wake is ~68KB\n  (rust2 #15)."
  },
  {
    "name": "edit",
    "description": "edit via content (minas apply); never compute positions",
    "body": "EDIT — content-resolved edits (the front door)\n\nUse:      minas delete <path>                       (delete a file — ADR-0060)\n          minas apply <path> <old> <new>\n          minas apply <path> --pair old.txt new.txt [--pair old2.txt new2.txt ...]\n                                            (recommended for batches: same atomic\n                                             contract as --hunks-stdin, but texts\n                                             come from files — no JSON escaping)\n          minas apply <path> --hunks-stdin   (JSON array of {\"old\",\"new\"} for many edits)\nWhat it does: Open -> find <old> -> verify (checksum + expected_text) -> replace -> Save,\nall in one command. Position math is done for you.\n\nRules\n- DELETE A FILE WITH `minas delete <path>`, not the shell. A module move creates the\n  new file (apply --whole-stdin) and then needs the old one gone; before this\n  existed the only way was `rm`, which is the one file-level operation minas did\n  not own (dogfooding #8). Contract: `deleted: <path>` on success, `NO-OP:` on\n  stderr for an already-absent path (exit 0 both — safe to re-run), exit 1 for a\n  refusal you cannot retry (`cannot delete …: not a regular file`, or\n  `read-only-base` under a comparison root), exit 2 for `delete-rejected …has\n  unsaved edits` (close the document, then retry). The daemon closes the document\n  it held and drops that path's outline/diagnostic caches, so a later\n  `check`/`outline` does not resurrect the deleted file.\n- Positions in minas are CHAR offsets (never bytes / UTF-16). Do not derive\n  them from byte lengths — a Japanese comment or emoji shifts everything\n  (rust2 #17). This is another reason to let apply resolve old->new.\n- Prefer content-resolved edits over positional ones. Measured: positional editing\n  (`minas edit` with computed char offsets) fails 0/5 vs 3/5 for content-resolved,\n  costs ~5x tokens, and can silently apply to the WRONG occurrence (a successful\n  exit with the wrong spot changed). Do not compute start/end char indices.\n- `minas edit` now REQUIRES expected_text: a positional edit without it can\n  silently corrupt (whole-document checksum cannot detect a shifted range;\n  rust2 #5-2). Give the current text at start..end, or use apply.\n- After an edit, DETECT errors with `minas check <path>` (ADR-0032): it waits\n  for LSP diagnostics and returns only errors — no full text, no parsing. Exit 2\n  means at least one error diagnostic (warnings alone exit 0); exit 0 is NOT a\n  verified clean (an empty check is always settled:false). A green verdict\n  requires the real build (cargo check / cargo test).\n- Locate before you edit: minas outline / minas at return the exact span of\n  the symbol you are changing, so your <old> text targets the right region\n  instead of a look-alike occurrence elsewhere.\n- `apply` MATCHES PLAIN SUBSTRINGS — `minas search -w` does not, so a `-w` checklist is NOT\\n  an apply plan. Measured (dogfooding #10, Python): a `search -w` census counted 16 `Note`\\n  occurrences (excluding `NoteTest`), 16 hunks were sent, and `apply` consumed one of them on\\n  the `Note` INSIDE `NoteTest` — the JSON still said `edits:16, edits_noop:0` while one real\\n  target was left unrenamed. minas now prints a `note: ... matched INSIDE the identifier ...`\\n  line when a match crosses an identifier boundary; treat it as \"your checklist and the matcher\\n  disagree\". Use a longer <old>, or verify with `minas search -w <file> <old>` after the loop.\\n- AFTER AN APPLY LOOP, VERIFY WITH `minas search <file> <old>` (expect 0 hits). In a language\\n  with no LSP server (Python) this is the ONLY leftover check — `references`/`rename` refuse,\\n  and `apply` reports only the span it changed (`chars a..b`), which says nothing about the\\n  occurrences it did not touch (dogfooding #6). For a module move, search the old SPECIFIER\\n  text in every importer (an import path is not a symbol).\\n- One invocation replaces the FIRST occurrence of <old>. Repeat for the next one,\n  or batch many changes with --pair / --hunks-stdin. Batching wins big because\n  each CLI invocation costs ~20ms of process start: measured 10 hunks in one\n  call vs 10 separate calls = 23ms vs 260ms (11x; rust2 #32). The multiplier\n  depends on CLI start cost, but batching is always at least as good.\n- Atomicity covers the buffer too, not just disk: before any edit is sent, all\n  old texts are verified in sequence; if one is missing the whole batch aborts\n  with nothing applied, and a later `read` returns the disk truth (rust2: no\n  phantom partial-apply). A repeated identical (old,new) pair hits occurrences\n  in order — N copies of the same hunk replace occurrences 1..N (rust2 #4-2).\n- --hunks-stdin reports the per-hunk outcome: the JSON has \"edits\" (accepted)\n  and \"edits_noop\", and hunk-level stderr lines distinguish two no-op kinds\n  (rust2 #10):\n    `HUNK SKIPPED: #N (old == new)` — you sent an identity hunk (same old and\n      new); it is your input mistake, nothing was applied.\n    `HUNK NO-OP: #N (changed nothing)` — the replacement left the text as-is\n      (defensive; an identity hunk is caught before sending).\n  Batching must not hide a silently-unapplied hunk — check \"edits_noop\" when\n  the diff you expected did not appear.\n- --pair is the file-based form of --hunks-stdin: each `--pair old.txt new.txt`\n  becomes one hunk, all applied on one connection and saved once (same atomic\n  contract — an undetected old aborts everything, disk untouched). Use it when\n  your old/new texts contain `\\n` or quotes and hand-escaping JSON is friction.\n- Small targeted replacements beat one huge <old> block: a big mismatch wipes too\n  much. For whole-file rewrites use --whole-stdin.\n- When machine-generating a hunk that PREPENDS to a multi-line <old>, the <new>\n  must be \"the prepended text + the ENTIRE old text, once\". Writing the doc line\n  and then repeating old's first line duplicates it (e.g. two `pub id: TodoId,`\n  fields -> E0124; rust2 #19). Prepend means insert before, not around.\n- MOVE A MODULE in two steps (`minas read <old> | minas apply <new> --whole-stdin` then\n  `minas delete <old>`) and remember: an **import specifier is not a symbol**, so\n  `references`/`rename` will NOT list the files that import the old path. After the move,\n  verify with the real build — measured (dogfooding #4, ts-backend) the move left two\n  TS2307 errors that only `tsc --noEmit` caught (TS has no `minas check`); `minas search`\n  on the old specifier text is the cheap pre-check.\n- MOVING THE LAST FILE OUT OF A DIRECTORY LEAVES THE EMPTY DIRECTORY. minas delete is\n  file-only (no directory surface, by design): leave it, or `rmdir` it with the shell and\n  say that you did (disclosed fallback, not silent).\n- A non-existent <path> is created as an empty file first (so Save can create\n  it); missing parent directories are created too (`apply src/domain/mod.rs\n  --whole-stdin` works even when src/domain/ does not exist yet).\n  A content-resolved edit needs <old> to already exist, so `apply <path>\n  <old> <new>` on a new file always ends in NOT FOUND — create content with\n  `apply <path> --whole-stdin` (or --whole). On failure the empty file is\n  removed again.\n- An empty whole-file replacement against a non-empty file is REFUSED unless\n  --allow-empty (subprocess stdin not connected is the usual cause; rust2\n  #5-1). To create a genuinely empty file, `touch` it or use --allow-empty.\n- THREE NO-OP SPELLINGS (stderr): `APPLY NO-OP: the replacement left the buffer unchanged`\\n  (a single `apply` whose old/new are identical or whose result is the same text),\\n  `HUNK NO-OP: #N` / `HUNK SKIPPED: #N (old == new)` (--hunks-stdin per hunk), and\\n  `NO-OP: <path> was already absent` (`minas delete`). stdout is unaffected: an unchanged\\n  buffer prints `no-change: <path> …` and exit stays 0 (dogfooding #8, A2).\\n- `applied: <path> …` on stdout means the buffer CHANGED. An identity edit (old == new) or a\n  whole-file replacement with the same text prints `no-change: <path> (the replacement left the\n  buffer unchanged)` instead (exit 0 both — idempotent). A verification script that greps\n  `^applied:` therefore cannot count a no-op as a change (dogfooding #6).\n- On rejection (exit 2) the message names the expected/found text and the range.\n  Re-read that spot fresh, fix, retry — do not blind-retry."
  },
  {
    "name": "outline",
    "description": "minas outline <path>: symbol tree (name/kind/span) without full text",
    "body": "OUTLINE — map a file's structure without reading it\n\nUse:      minas outline <path>\n          minas outline <path> --recursive [--depth N]  (cross files — ADR-0049)\nOutput:   compact JSON tree, no file text: each symbol is\n          {\"name\", \"kind\", \"range\", \"selection_range\", [\"path\"], \"children\"},\n          kinds: Module/Function/Method/Type/Enum/Constant/Variable/Other\n          (\"path\" appears only where the file changes — see --recursive below)\n\nRules\n- On an unfamiliar file, outline FIRST: every symbol's name, kind, and exact\n  span for a fraction of a read. Measured (7,981-line file): outline + at =\n  55 KB vs a full minas get = 814 KB (~93% fewer bytes).\n- --recursive follows file-scoped modules (`mod name;` → the file that defines\n  them) and inlines their symbols as children — one call maps a whole crate\n  instead of one outline per file. Default depth 3; --depth N implies --recursive\n  and sets the cap (--depth 1 = direct child modules only).\n  `--depth` counts MODULE HOPS (file→file), not symbol nesting levels: a direct\n  child module is inlined whole at --depth 1, including its nested impls/enums,\n  so on a crate whose modules are one level deep every --depth >= 1 gives the\n  same tree (that is correct, not a broken flag — dogfooding #9). A \"truncated\" note on\n  stderr means the 500-symbol cap cut the tree (still exit 0; per-file outline\n  for the parts you need).\n- A RANGE IS MEANINGLESS WITHOUT ITS FILE. --recursive inlines symbols from\n  other files, so a node's offsets can live in a different file than its parent's\n  (ADR-0057). Resolve a node's file as: the node's own \"path\" if present, else its\n  parent's file, else the top-level file you passed. Only nodes whose file CHANGES\n  carry \"path\" (the children inlined from an expanded module); the module node\n  itself stays in the parent file. Resolve the file FIRST, then address the range\n  against it: `minas read <file> --span a:b` (char offsets, as returned) or, if you have line\n  numbers from another command, `minas read <file> --lines a:b`.\n  Applying a recursive child's range to the parent path silently edits the wrong\n  region.\n- range and selection_range are objects {\"anchor\",\"head\"} holding char offsets\n  (the same unit as DocumentEdit positions); selection_range is only the name\n  token. They are CHAR OFFSETS for `minas read --span <anchor>:<head>` and for `minas edit`\n  — NOT `--lines` addresses (those are 1-origin line numbers). Use the span to read at symbol\n  granularity, and minas apply with the text you then see — read at symbol\n  granularity, not file granularity.\n- The tree is nested and grep-able: find the function you need by name instead\n  of scanning text.\n- The first outline on a path is cold (seconds: project load + LSP settle);\n  the daemon caches it, so pull it once at session start and reuse the ranges\n  for reads / at / edits. Warm calls are ~0.1s.\n- Exit 1 = not supported (no LSP server for this language) or bad input;\n  exit 2 = retryable LSP error (retry once later, then give up).\n- REVISION: for a single file (`minas outline <file>`) the command prints\n  `outline: revision <n>` on stderr — that is the fnv1a64 of the file text the ranges\n  were computed from, the same number `read`/`get --lines` print as `\"checksum\"`.\n  Compare them before feeding an old range to `read --span` / `minas edit` (ADR-0063).\n  `--recursive` prints no revision: its tree spans files, so resolve each node's\n  `path` and compare that file's own read checksum.\n- KIND IS THE SERVER'S: `kind` is mapped from the LSP symbol kind, so a TS type alias\n  (`export type X = …`) can come back as `variable` and an `interface` as `type`\n  (tsserver's classification). Branch on the name/range, not only on `kind`, when the\n  language is not Rust (dogfooding self-host #9).\n- NO IMPORT FOLLOWING: `--recursive` follows Rust `mod name;` declarations. In TS a\n  file's imports are not followed (`import … from \"./x\"` is not a module declaration),\n  and the project's file set is defined by tsconfig `include`/`files` — so for TS use\n  the project's own enumerator (`rg --files`) and per-file `outline` (self-host #9).\n- `<unknown>` AS A NAME: some servers emit a node whose name is literally `<unknown>` (measured:\n  typescript-language-server on `module.exports = { … }` in a `.cjs`). It is the server's string,\n  not a symbol you can rename — treat it as an anonymous node and use its range/children."
  },
  {
    "name": "at",
    "description": "minas at <path> <line>:<col>: enclosing symbol + its exact range",
    "body": "AT — the symbol enclosing a position, with its exact range\n\nUse:      minas at <path> <line>:<col>   (1-origin, like get --lines)\n\nUnits: all positions minas exposes are CHAR offsets (not bytes, not UTF-16).\nNever compute offsets from byte length — a Japanese comment or an emoji\n(1 char / 4 bytes) shifts them immediately. Better still: let apply resolve\nthe old text and never compute a position yourself (rust2 #17).\nRange vs selection_range: `range` is the whole item INCLUDING attributes\n(e.g. a `#[allow(...)]` above an fn), `selection_range` is the name token.\nUse `range` for `read --span` / `minas edit` and expect it to start before the attribute\nblock (rust2 #17). It is NOT a `--lines` address: `--lines` takes 1-origin LINE numbers, so an\noffset there yields `lines: []` + exit 0 (measured 2026-09-15).\nOutput:   pretty JSON, no file text: {\"name\", \"kind\", \"range\",\n          \"selection_range\", \"found\"}. found=false is a SUCCESS (exit 0):\n          the position is in no symbol — pick a different spot.\n\nRules\n- Before touching a spot, ask what it belongs to: at names the enclosing\n  function/type and its exact span, so your read and your <old> text are\n  scoped to the right region — no full-file reads, no guessing which function\n  a matching string lives in.\n- A COLUMN PAST THE END OF THE LINE IS CLAMPED, not rejected: `at <file> 7:60` on a 30-char\n  line still names the enclosing symbol (`found: true`, exit 0). So `at` cannot validate a column —\n  to check one, use `hover <line>:<col>` (an empty answer means nothing is there). Measured\n  2026-09-15: a byte-derived column and the correct char column both returned the same symbol.\n- A RANGE STARTS AT THE ITEM'S FIRST TOKEN: indentation is NOT part of it (a doc comment or\n  attribute above the item may be). When you replace a span, do not re-add the leading whitespace\n  — measured 2026-09-15: a positional edit that re-inserted 4 spaces produced a file that no\n  longer compiled, exit 0 (cargo caught it, minas did not; #17 was the reverse direction: minas\n  reported failure for an edit that HAD landed).\n- selection_range is the substring of the symbol NAME: the address for\n  rename-style edits and for locating the token named in a rejection message.\n- Ranges are {\"anchor\",\"head\"} objects of char offsets: pair the span with\n  `minas read --span <anchor>:<head>` to see the actual lines before editing inside it.\n- Warm after an outline of that path: ~0.1s (measured 6.5s -> 0.09s cold to\n  cached); the cold cost is paid by the one session-start outline, not by\n  every at.\n- Exit 1/2 as with outline/rename (1 = not supported / bad input, 2 =\n  retryable LSP error)."
  },
  {
    "name": "rename",
    "description": "minas rename <path> <old> <new> (semantic); apply for a few",
    "body": "RENAME — semantic rename vs apply (measured decision)\n\nminas has a built-in semantic rename (content-addressed, ADR-0029):\n    minas rename <path> <old> <new>\n- Use it for renames with MANY occurrences or MULTIPLE files. Measured (3 files,\n  21 occurrences): LSP rename 5/5 success vs apply loop 2/5 (apply kept missing\n  occurrences), ~half the tokens (-49%) and cost (-57%).\n- It renames the definition and ALL references (imports, calls) in one call,\n  saves to disk, and prints the impact: `renamed: old -> new (N files, M edits)`\n  plus a `changed:` list — verify the impact is what you meant.\n- It renames IDENTIFIERS only: string literals, comments, macro-generated\n  text, JSON/config keys and serde field names-as-strings are NOT touched.\n  Measured (rust2 #39): renaming a struct field updated all 43 identifier\n  references across 5 files, but JSON literals asserting the wire field name\n  kept the old spelling. After a rename of something that appears in text too,\n  grep for the old name and fix the literals by hand.\n- Same-named but different symbols are left alone: a `version: u64` parameter\n  and an `expected_version` field were untouched when renaming the `version`\n  field (rust2 #39) — resolution is semantic, not textual.\n- UNLINKED TARGET (2026-09-15): renaming inside a file that is not part of any crate/module\n  tree (no `mod x;` yet, or outside the crate graph) is refused by the server as\n  `No references found at position (code -32602)`. The raw wording implies the request can\n  never succeed; minas adds a `note:` naming `unlinked-unverified` (one `check` call on the\n  failure path), so add the declaration and retry — that failure DOES change once linked.\n- Exit 1 = not supported / bad input (do not retry), exit 2 = retryable\n  (symbol not found, LSP error, stale analysis — re-read and retry).\n\nIf only content-editing is available (minas apply):\n- For a handful of occurrences in ONE file, loop with apply. Measured on a small\n  file it is as cheap as LSP rename.\n- For many occurrences or cross-file renames you MUST verify with a final\n  `minas references <path> <old>` (or grep/read) that no old name remains —\n  measured failure mode is silently leaving one occurrence behind.\n  In a language with no LSP server (Python), `references` refuses too: the leftover check is\n  `minas search <file> <old>` (0 hits = nothing left) plus the project's own gate —\n  `rename`-by-apply has no automatic net (dogfooding #6).\n\n- USE / IMPORT ANCHORS ARE REFUSED (2026-09-15): when the FIRST occurrence of <old> in <path>\n  sits on a `use` / `import` / `from` line (a re-export, or an import binding), minas refuses with\n  exit 1 and names the definition, e.g. `import/re-export anchor: src/domain/entry/mod.rs:2 is a\n  use/import binding of `EntryId`, not its definition (defined at src/domain/entry/id.rs:1) …\n  minas rename <defining file> EntryId <new>`. Reason: the server renames THAT BINDING — for a\n  `pub use` it writes `pub use id::EntryId as EntryKey;`, leaves every consumer of the old name\n  behind, and the crate stops building (measured: exit 0 + an INCOMPLETE note that blamed\n  comments/strings while 2 importers broke with E0432). A definition anchor or a call site\n  (`crate::…::f()` / `self.m()`) is unaffected — measured: a call-site anchor renamed 2 files /\n  2 edits with zero residue. This is NOT a retryable failure: the same request rewrites the same\n  binding.\nIMPORT ALIASES: an anchor that IS an alias (`use x::y as z` — the `z` token) is refused by the\nserver, not silently half-renamed: rust-analyzer answers `Renaming aliases is currently\nunsupported`, minas reports that message verbatim and exits 1 (do not retry; the same request\nfails the same way). Rename the DEFINITION instead (`minas rename <defining file> <name> <new>`):\nmeasured (dogfooding #2) that rewrites the `use … as z` line and leaves the aliased call sites\nalone, which is the correct semantic for a local binding. `references` on the same alias anchor\nreturns the underlying item's list and marks the status line INCOMPLETE (see below) — do not read\nthat count as the alias's whole impact.\n\nNever try to re-implement the rename by hand-editing each call site when an LSP\nrename tool exists.\n- SCOPE: the rewrite comes from the language server, so it only covers the code\n  that is IN THE ACTIVE cargo configuration. `#[cfg(feature = \"x\")]`-gated\n  modules and files outside the crate graph are invisible: a rename can report\n  `1 files, 3 edits` and still leave a use behind, which `cargo build` will not\n  catch but `cargo build --features x` will (dogfooding #16). To catch that,\n  minas scans the renamed file's own directory afterwards and prints\n  `warning: N file(s) still mention '<old>' but were not touched by the LSP\n  rename …` on stderr with the paths (exit stays 0). Treat that warning as part\n  of the result: for a feature-gated crate, verify with\n  `cargo build --all-features` (or run `minas search <file> <old>` yourself).\n  The scan covers the whole **workspace** the file belongs to (it walks up to the\n  outermost Cargo.toml), because in a multi-crate workspace the consumer is\n  usually in another crate — that was the original gap (dogfooding #16 follow-up:\n  a cfg-gated file in crate a calling into crate b was missed when only the\n  renamed file's own directory was scanned). It is text-level, so a mention in a\n  comment or string also warns; capped at 500 files / 1 MiB each.\n- IN-FILE DELTA: the textual net also counts the mentions it finds INSIDE the files\n  the LSP did touch, and reports them as `note: N more textual mention(s) of '<old>'\n  inside the file(s) … (string literals / comments)`. Without it, a file that produced\n  hits looked fully covered while its assert-message strings kept the old name\n  (measured: 33/38 in-file recall on the largest source file, dogfooding self-host #4).\n- INCOMPLETE ON STDOUT: the impact line can read\n  `renamed: a -> b (N files, M edits) — INCOMPLETE: K file(s) still mention 'a' and were NOT\n  changed (see stderr)` or `— INCOMPLETE: K whole-word mention(s) of 'a' remain inside the changed\n  files` (mentions the server left inside files it DID touch — comments/strings, or references it\n  did not see; dogfooding #2). That means the LSP saw only ONE session's view of the project (for TS/JS\n  the same root can have several languageId sessions, e.g. `.tsx` + `.jsx`) or the file is outside\n  its file set. **Do not treat \"N files, M edits\" as the whole blast radius** when that marker is\n  present. But do not read it as \"failed\": the listed mentions are TEXT-LEVEL (comments, strings, or a\n  DIFFERENT identifier of the same name). Measured (dogfooding #9): renaming the common word `line`\n  printed `INCOMPLETE: 3 file(s)` where every hit was prose or an unrelated `line` local (0 real\n  leftovers) while the rename was complete (`search` 0 hits, tests + clippy green). Policy: read each\n  listed file with `minas search -w <file> <old>`, fix only real leftovers, and never make \"no\n  INCOMPLETE\" a CI condition — a common word always trips the net (ADR-0067: the marker remains the\n  only in-band signal for a genuinely missed file, which is why it is not suppressible). The stderr\n  list names the files.\n  STRONGER CHECK THAN `search -w`: if another symbol in the workspace has the same name, query it from\n  its own definition — `minas references <that definition's file> <old>`. If the warned files are\n  exactly its reference list, the marker is explained by that other symbol (measured, dogfooding #9:\n  a rename warned about 3 files, and `references src/json.rs render` returned exactly those 3 files —\n  0 real leftovers). `search -w` cannot tell `Value::render` from a leftover; the reference list can.\n- The stderr net's parentheticals are language-neutral: `not seen by the LSP` (another\n  language/session, outside the file set, or a comment/string) and, for mentions inside files the\n  LSP DID touch, `the LSP left them` — an IDENTIFIER among those is a real leftover, not\n  decoration (measured: a live `import { Card }` specifier was left behind)."
  },
  {
    "name": "references",
    "description": "minas references <path> <old>: list a symbol's references",
    "body": "REFERENCES — impact check before/after a rename (ADR-0029)\n\n    minas references <path> <old>\n\n- Resolves <old> like rename (first identifier occurrence) and lists every\n  reference with the definition: `path:line` (1-origin), e.g.\n- ALIAS ANCHORS: if the resolved token is the alias side of `... as <name>` (Rust\n  `use x::y as z`, TS `import { y as z }`, Python `from x import y as z`), minas ALSO\n  queries the original name on the same declaration line and returns the UNION. The server\n  answers differently per token (measured tsserver: specifier = 8 locations/3 files, alias =\n  2 locations/1 file), so trusting the alias token alone silently dropped a whole consumer\n  file (dogfooding #2, TS). `import * as ns` has no original name and is queried as-is.\n- COUNT SEMANTICS: the list is deduplicated by `path:line` — two references on the same line\n  (e.g. `import { y as z }` has two tokens on one line) appear ONCE, so N counts distinct\n  positions, not locations returned by the server.\n- Resolution rule: <old> is matched as an identifier and resolved to the FIRST\n  such occurrence in <path> (comments/strings excluded). With several\n  same-named fields in one file, point <path> at the definition's file so the\n  first occurrence there is the one you mean — the daemon does not guess which\n  of several identical names you wanted.\n      2 references in 1 files:\n      /abs/path.rs:4\n      /abs/path.rs:1\n- Use before a rename to see what will change, or after apply-based edits to\n  verify nothing was missed — the T5 failure mode is a silently-left occurrence.\n- The output is locations only (token-cheap): read a location with\n  `minas get --lines <line>:<line>`.\n- Exit 1/2 as with rename.\n- SCOPE: same limitation as rename (dogfooding #16) — the list comes from the\n  language server, so a use inside `#[cfg(feature = \"x\")]`-gated code is not\n  included. minas scans the whole workspace (outermost Cargo.toml) afterwards and\n  warns on stderr (`warning: N file(s) still mention '<old>' …`) when the name\n  survives in a file the LSP did not list; exit stays 0.\n- INCOMPLETE ON STDOUT: the count line can read `N references in M files: — INCOMPLETE: K file(s) still\n  mention '<old>' and are not listed (see stderr)` (a file the server's list omitted) or\n  `— INCOMPLETE: K whole-word mention(s) of '<old>' inside the listed files are missing from this list`\n  (a mention inside a file the list DID include). The second form is the one that matters when the\n  symbol is reached through an import alias: measured (dogfooding #2) `references <file> alias` listed\n  3 locations and silently left the alias's live call site out — the count line is now a lower bound\n  whenever INCOMPLETE appears. Verify with `minas search -w <file> <old>`, or query the definition anchor instead. NOTE: the\n  listed mentions are TEXT-LEVEL — comments, strings, or a DIFFERENT identifier of the same name\n  (measured, dogfooding #9: querying the common word `line` listed 3 files whose hits were 100% prose +\n  unrelated `line` locals, 0 real leftovers, and the tool was right). Treat the marker as \"verify\",\n  IN-FILE RESIDUAL IS COUNTED PER LINE (2026-09-15): a line the list already points at\n  contributes 0 to INCOMPLETE — the list shows one `path:line` per line, so a second mention on\n  the same line has no position of its own and is reported separately as an informational\n  `note: N line(s) … carry more than one mention` (measured: `assert!(EntryId(2) < EntryId(10))`\n  made a clean symbol look dirty). Only lines the list does not mention at all count. The\n  comparison canonicalizes paths (`/tmp` vs `/private/tmp` no longer produce a false \"file not\n  listed\" warning) and the list is printed sorted by path, then line. For the sweep these notes\n  ask for, use the crate-wide form: `minas search --crate-root src/lib.rs -w <old>`.\n  not \"failed\", and do not make \"no INCOMPLETE\" a CI condition. The file-level form means one session's\n  view (TS/JS languageId split), the alias-anchor case below, or a file outside the file set (ADR-0067)."
  },
  {
    "name": "check",
    "description": "minas check <path>...: wait for diagnostics, returns only errors",
    "body": "CHECK — find errors fast; it never certifies clean (ADR-0032)\n\nUse:      minas check <path> [<path> ...]\n          minas check --crate-root src/lib.rs [--include-tests]\n                                    (--include-tests adds the tests/ DIRECTORY to the\n                                     expansion; it does NOT enable the cfg(test) cfg, so a\n                                     test-only error stays clean-unverified — cargo test is\n                                     the gate for test code, 2026-09-15)\n                                    (expand to every .rs under src/ [+ tests/]\n                                     and check them all in one call — the\n                                     edit→verify loop for multi-file breakage)\nOutput:   compact JSON, no file text: an array with one entry per path:\n          {\"path\", \"total\", \"diagnostics\", \"settled\", \"verdict\"}, where each\n          diagnostic is {\"severity\", \"line\" (1-origin), \"col\" (1-origin, char\n          units), \"start\", \"end\", \"message\"}. A path whose pull FAILED still\n          gets an entry, carrying \"error\" (total 0, settled false, verdict\n          \"failed\") — so an empty array never means \"all paths failed\"\n          (ADR-0046). verdict is `errors (n)` / `warnings (n)` / `hints (n)` /\n          `clean-unverified` / `unlinked-unverified` / `failed`, counted by\n          diagnostic severity — branch on it directly (ADR-0045/0052).\n          `unlinked-unverified` means the LSP has NOTHING TO ANALYZE for that\n          path — either it is outside any project (no ancestor Cargo.toml /\n          package.json / pyproject.toml / go.mod / .git, rust2 #21) or the\n          server says the file is not part of any crate/module tree\n          (rust-analyzer's `unlinked-file`). A file with real errors returns\n          total 0 / one Hint in both cases — NEVER read it as clean, and DO NOT\n          commit on it. This is what a just-created file looks like for a few\n          seconds: create it, link it, then re-run check to get the real\n          verdict (dogfooding #3). Hints/Info\n          (RA-only: inactive #[cfg] branches etc.) are never counted as\n          warnings: check reports LSP diagnostics, not rustc lints, so\n          \"warnings (1)\" here means an actual Warning diagnostic, not an\n          unused-import lint (rust2 #9).\n          `--summary` prints one `path: verdict` line per path instead of the\n          JSON array (exit codes unchanged).\nExit:     0 = no error diagnostic was found — NOT a verified clean (see below),\n          2 = anything that is not \"looked and found no error\": at least one\n              error diagnostic, a retryable LSP failure, or a verdict that could\n              not look (`unlinked-unverified`, `failed`),\n          1 = not supported / bad input (never retry these).\n          Branch on 0-vs-2, not on the verdict text alone: unlinked/failed are 2.\n\nRules\n- check FINDS errors; it cannot PROVE the code is clean. Exit 2 is\n  authoritative (a real error); exit 0 is not green. A green verdict requires\n  the real build (cargo check / cargo test). Measured (dogfooding #9): a 19-file\n  `--crate-root --include-tests` expand returned `clean-unverified` for EVERY file (exit 0)\n  while `cargo check` failed on the bin target (E0423: expected function, found module) — and\n  a deliberate E0308 inserted into the SAME file was caught. Missing name-resolution errors\n  while catching type errors in one file is the normal shape, so `check` alone is never an\n  exit condition for an edit loop.\n- `--include-tests` IS A PATH EXPANSION, NOT A CFG: it adds `tests/*.rs`, but the analysis\n  still runs WITHOUT `cfg(test)`. Measured 2026-09-15: a broken test-only import left\n  `--crate-root --include-tests` at 31/31 `clean-unverified` exit 0 while `cargo test --no-run`\n  exited 101 (E0433). Read it as \"also look at the tests/ directory\", never as \"my tests are\n  checked\".\n- CHECK ANALYSES, IT DOES NOT RUN: the LSP type-checks and reports diagnostics, so\n  \"check clean\" and \"suite green\" are separate bars — a test whose assertion is\n  simply wrong passes check and only fails in `cargo test` (measured: two new tests\n  in mina-text passed `check --crate-root` clean and their correctness was only\n  decidable by the suite). Never report a test as verified on a check verdict.\n- After an edit (minas apply / edit), run check INSTEAD of wait + get +\n  JSON-parsing the snapshot: it waits for LSP diagnostics to settle and returns\n  only the diagnostics — the wait, the full text and the parsing are gone. It\n  replaces the snapshot round trip, NOT the build.\n- Pass multiple paths to verify every file you touched in one call\n  (ADR-0046): `minas check src/lib.rs src/other.rs`. When a cross-cutting\n  change (new field, new enum variant) breaks several files at once and you\n  cannot enumerate them, use --crate-root: it checks the whole crate tree\n  (src/) plus tests/ with --include-tests in one round trip (rust2 #7).\n- Read \"error\" on an entry before concluding anything from \"total\": it means\n  the pull failed (not \"no errors\"), and the message names the reason (session\n  lock busy = retry, server dead = the next .rs Open respawns it).\n- TRANSIENT `failed` RIGHT AFTER A MODULE-TREE CHANGE: exit 2 does NOT mean \"errors found\" —\n  a `failed` verdict can repeat while the server re-analyzes the crate (measured, dogfooding\n  #9: adding several `mod` lines made two consecutive checks return `failed` at ~0.5s each,\n  and the next call was clean). Policy: on `failed`, read the entry's `error`, re-run once or\n  twice (or again after the next edit), and only then escalate — never conclude \"errors\" or\n  \"clean\" from the exit code alone; `cargo check` stays the verdict for the changed tree.\n- The 1-origin line is the address for `minas get --lines <n>:<n>`; the\n  char range locates the span whose text you paste as the <old> argument of\n  `minas apply`. The line:col pair is the address for\n  `minas at / peek / hover <path> <line>:<col>` — pass it straight through\n  without computing anything (ADR-0047).\n- It is an LSP fast path (incremental), not a compiler: for semantics beyond\n  the LSP (borrow checker etc.) still run the real build.\n- check reports only rust-analyzer's diagnostics. rustc lints that RA does not\n  compute (unused imports, dead_code, unreachable_patterns, deprecated usage,\n  ...) never appear — they are invisible to check's pull (verified 2026-09-12:\n  unused import / unused variable / unreachable pattern / deprecated call /\n  never-read field / unused arg all returned clean-unverified). Even a genuine\n  `warnings (n)` verdict is rare in practice: RA's semantic pull is almost all\n  Error + Hint (inactive #[cfg] code), and Warning requires the LSP server to\n  actually emit one. Zero warnings is something only the real build (cargo\n  check with warnings) can show; check is an ERROR-detection shortcut, never a\n  warning detector.\n- settled is not a clean/not-clean flag. An empty result is ALWAYS\n  settled=false, even for a genuinely clean file — settled=true requires a\n  non-empty set to repeat (ADR-0045/0052). Empty means \"the pull reported\n  nothing\": it may be clean, or it may be an error the pull misses. It comes\n  back fast (~0.6s) instead of burning a ~10s budget, and waiting would not\n  turn it into a clean (ADR-0052). The verdict \"clean-unverified\" is the\n  machine-readable spelling of that state — treat it as \"run cargo\", never as\n  green.\n- rust-analyzer's pull misses real errors (name typos, method resolution, unresolved\n  imports / E0432, duplicate definitions / E0428, cross-file type errors come back empty), so an empty result is never a\n  reason to skip the build. Measured (dogfooding #9, a 9-file crate-root expand): the same\n  run reported `errors (1)` for a file with E0308 (mismatched types) and `clean-unverified`\n  for the file whose E0432 the compiler was failing on — one pull can be right for one file\n  and empty for another in the same change.\n- PROJECT SCOPE: the verdict is about the file's own cargo project, and it looks\n  identical whether the file is a workspace member or a stray/scratch package inside\n  the tree. minas does NOT annotate this: telling a member crate from an unrelated\n  package needs cargo's membership rules (members globs + path dependencies), and a\n  note that cannot tell them apart misfires on the normal case (checking a member\n  crate from the repo root). So: when you check a path outside the tree you are\n  editing, read the verdict as being about THAT project. A file whose package is not\n  in the workspace's crate graph comes back `unlinked-unverified`, which is the\n  honest answer (dogfooding self-host #7).\n- `--crate-root` IS RUST-ONLY: it takes an entry `.rs` and expands to every `.rs` under\n  `src/`. Given a `.py`/`.ts` path it refuses upfront with\n  `Error: --crate-root takes a .rs file (e.g. src/lib.rs)`, exit 1 — honest, but do not read it\n  as \"check is unavailable\": the per-path `minas check <file>` form still answers (and is where\n  the transport limitation below shows up). For non-Rust languages the green bar is the\n  project's own gate (`tsc --noEmit`, `python -m unittest`, `cargo check`).\n- TRANSPORT: `check` is a **pull** implementation (`textDocument/diagnostic`), so it\n  works only for servers that advertise `diagnosticProvider` — rust-analyzer does,\n  **typescript-language-server does not** (it publishes diagnostics by push). For TS/JS\n  the answer is `check not supported … (the LSP server does not advertise\n  diagnosticProvider)`, exit 1, and there is no minas verification command for that\n  language family yet: use the project's own gate (`npx tsc --noEmit`, `vitest`,\n  `cargo check`) and say so when you do. A push-based check is backlogged\n  (dogfooding self-host #2).\n- SCOPE BY SERVER: `check`'s verdicts are only as good as the server's diagnostics. For\n  a server that cannot answer at all, `failed` + exit 1 means \"cannot look\", and exit 2\n  means \"looked and could not settle\" — do not read either as clean."
  },
  {
    "name": "hover",
    "description": "minas hover <path> <line>:<col>: type & signature without full text",
    "body": "HOVER — type/signature lookup without reading (ADR-0032)\n\nUse:      minas hover <path> <line>:<col>   (1-origin, like get --lines)\nOutput:   compact JSON: {\"path\", \"text\"} — type + signature + doc, no file text.\n          Empty text is a SUCCESS (exit 0): nothing to hover at that position\n          (whitespace, comments).\n\nRules\n- Before reading a definition to learn its type, hover the call site: the\n  server returns the signature in one round trip (token-cheap).\n- The doc comment is truncated (<=2000 chars) — the type/signature is the\n  reliable part; read the doc only when the short part is not enough.\n- Pair with minas peek <path> <line>:<col> to jump to the definition.\n- Exit 1/2 as with outline/at (1 = not supported / bad input, 2 = retryable)."
  },
  {
    "name": "symbol",
    "description": "minas symbol <path> <query>: find where a name lives in the workspace",
    "body": "SYMBOL — workspace search instead of rg (ADR-0032)\n\nUse:      minas symbol <path> <query>\nOutput:   compact JSON array, no file text: each hit is\n          {\"name\", \"kind\", \"path\", \"line\" (1-origin)} — read the location\n          with `minas get --lines`.\n\nRules\n- <path> is any file in the workspace (it anchors the workspace root);\n  results can span the whole root, not just that file.\n- Use it INSTEAD of rg when you need to know WHERE a name is defined or\n  declared: the server returns exact symbols (no whole lines, no false\n  positive comments/strings), and the hit range is the definition.\n- Fuzzy matching varies by server; a broad query then narrowing by kind/line\n  is cheaper than many greps.\n- Exit 1 = not supported / bad input (empty query), 2 = retryable.\n- SERVER BLIND SPOT (.cjs/.mjs members): tsserver's workspace/symbol does not index\n  members of plain-JS files (`module.exports = { parseQueryLike }` in a .cjs), so\n  `symbol <that .cjs> parseQueryLike` returns [] exit 0 while `outline` / `at` / `hover` /\n  `references` / `rename` all work on the same name (measured, dogfooding #4; the raw\n  server returns 0 hits too, so it is a blind spot, not a minas loss). For those names use\n  `outline` + `at` instead of `symbol`.\n- SCOPE: the result set is the LANGUAGE SERVER's workspace/symbol, which follows the\n  crate graph — so hits can come from dependency sources OUTSIDE your repo\n  (`~/.cargo/registry/...`, `node_modules/...`). The `path` is the only thing that\n  tells you; minas prints a stderr note when hits fall outside the anchor file's\n  project root (`note: N of M hit(s) are outside the project root …`). It is a\n  definitions-only search: it cannot answer \"what handles X\" (that is a pattern\n  question — `rg`, or `minas references` when X is a symbol). (dogfooding self-host #2)\n"
  },
  {
    "name": "persist",
    "description": "minas edit leaves the buffer dirty; save explicitly",
    "body": "PERSIST — when changes hit the disk\n\n- `minas apply` (and --hunks-stdin) Save for you: after success the file on\n  disk is updated.\n- Raw `minas edit --path <f>` does NOT save: the daemon buffer changes but the disk file\n  stays old (dirty=true). `--path` is REQUIRED and verified against the daemon's current document before the edit is\n  sent (a path-less positional edit lands in whatever document the daemon has focused — with two\n  panes on one daemon that is not necessarily yours, and a later `exec '\"Save\"'` then persists the\n  wrong file; measured 2026-09-15). The success path also prints `note: edited <path> (buffer\n  only)`. If you used edit, persist with:\n      minas exec '\"Save\"' --brief     (a 29-byte buffer: 1.2 KB with --brief vs 61 KB\n                                       without — the snapshot carries the daemon's whole\n                                       event history; measured 2026-09-15)\n- When an edit succeeded but the buffer is dirty, minas prints a stderr note that names a\n  runnable command (`minas exec '\"Save\"' --brief` — before 2026-09-15 it said `session exec`,\n  which is not a subcommand):\n  \"buffer is dirty (not saved); persist with: minas exec '\"Save\"'\".\n- Before relying on a file's on-disk content, prefer apply (which re-opens the\n  file fresh) over mixing edit + assumptions."
  },
  {
    "name": "errors",
    "description": "exit codes 0/1/2 and how to recover from rejections",
    "body": "ERRORS — exit codes and recovery\n\nExit codes (applies to minas apply / edit / hunks):\n  0  success (applied, or no-op)\n  1  usage/CLI error — fix the arguments; do not retry the same call\n  2  retryable failure — old text not found, checksum/expected_text mismatch,\n     empty whole-file replacement refused (pass --allow-empty to truncate),\n     or Save failed. Re-read the file fresh, then retry.\n\nBlocking `minas wait <generation>` returns the full state snapshot when the\ngeneration moves — use `minas wait --brief <generation>` to strip the history\nfields (events/activity) from every wake; the wait loop pays this cost per\nwake (rust2 #15: 68KB -> ~1KB). The daemon settles content changes before\nreturning (rust2 #16/#18): an edit batch (apply = Open -> replace_range ->\nSave) wakes it only after the text stops changing (~120ms quiet), so the\nreturned text is the settled one, not the pre-edit text at the Open stage.\nIf the last content change is already older than the quiet window the wait\nreturns immediately (~10ms), so `wait <old generation>` stays a cheap check.\nIf no content change ever arrives it falls back after ~500ms (ADR-0014's\nimmediate-return contract is preserved).\n\nSemantic commands (rename / references / outline / at / hover / symbol / check)\nshare 0/1/2 but classify differently: exit 1 = not supported / invalid input\n(do not retry), exit 2 = retryable (symbol not found, LSP error — re-run once\nlater). Exceptions:\n- minas at with no enclosing symbol is exit 0 (found=false), not an error —\n  only the LSP request itself can fail.\n- minas hover with nothing at the position is exit 0 (empty text), not an error.\n- minas check returns exit 2 when at least one error diagnostic is present\n  (warnings alone exit 0). So $?=1 = not supported / cannot open (do not\n  retry), $?=2 = error found or a retryable LSP failure, $?=0 = no error found\n  — and $?=0 is NOT a verified clean (an empty check is always settled:false):\n  branch on $? for errors, but a green verdict requires the real build (cargo).\n  Same classification as outline / at / rename (rust2 #23).\n\nrename failure lines are prefixed `rename failed:` (success is\n`renamed: old -> new (N files, M edits)`) so the two are never confused\n(rust2 #23).\n\nRejections are the editor telling you what changed:\n- \"document changed since read\"      -> the file moved; re-read and retry.\n- \"expected text mismatch: expected X,\n  found Y at [lo,hi)\" -> the range is not\n  what you assumed; re-read THAT range (use --lines) and fix the old text.\n- \"NOT FOUND: text\"                  -> the old string is not in the file; re-grep.\n\nRecovery loop: read the reported spot with `minas get --lines`, correct the\nold/new, retry. Never blind-retry a rejected edit.\n\nExternal modification: the daemon auto-reloads files changed on disk (2s poll;\na reload detected by a command's Open path appears sooner). If the new size is\nunder half the old, the reload sets snapshot status \"file shrank N -> M chars\"\n(rust2 #5-5/#6/#8) — it appears in the status field of the NEXT snapshot\n(get / get --brief / apply response) and in the activity log. The status is a\nGLOBAL one-shot (any document's snapshot may carry it), so it embeds the\naffected path: \"reloaded from disk (path: /abs/path.rs; ...)\" — read the path\nbefore acting. After any external tool touches a file you are editing, run\n`minas get --brief` and read its status. The reload replaces the text AND its\nundo history, so a truncated file cannot be undone; recover by re-applying\nfrom a known-good source (git show > file, then apply). Note: the reload is watcher-\ndriven (2s poll), so the warning appears on the get --brief AFTER it fires — a\nget run immediately after the external change may still show status None (rust2\n#9). `minas read` does not trigger a reload (open document wins); only the\nwatcher / an Open on a changed file does."
  },
  {
    "name": "exec",
    "description": "minas exec '<Command JSON>': raw command passthrough + the headless allowlist",
    "body": "EXEC — raw command passthrough (advanced)\n\nUse:      minas exec '<Command JSON>'        (examples: `\"Save\"`, `\"GetState\"`)\n          minas exec '\"Save\"'                (the argument IS the JSON string)\n\nWhat it does: sends one Command and prints the raw response. A snapshot response\nprints the StateSnapshot JSON (same as before); lightweight responses (Hints /\nPeek / ReviewComments / ServerInfo / Outline ...) print that response JSON\n(rust2 #26).\n\nHeadless allowlist — anything else is rejected with status\n\"headless clients can only use ...\" and minas exits 1:\n    GetState, Save, WaitFor, DocumentEdit, Open, Close,\n    ListReviewComments, ClearReviewComments,\n    RegisterBaseRoot, UnregisterBaseRoot\n- A rejected command is a NO-OP and exits 1 (rust2 #26). Do not treat the printed\n  snapshot's status as success — check $?.\n- TUI-only commands (Search, SetMode, Insert-mode keys, selection/view moves) are\n  not on the allowlist. Use the dedicated subcommand instead: review, hover,\n  outline, peek, hints, at, symbol, rename, references, check.\n- RegisterBaseRoot / UnregisterBaseRoot have no dedicated subcommand — exec is\n  the only way (ADR #53). They register a read-only comparison root.\n  Two things to know before you use it (dogfooding #12):\n  * the registered subtree becomes READ-ONLY: every later apply/edit under it\n    fails with `EDIT REJECTED: read-only-base: <path> …`, exit 2 (reads still\n    work). Registering the tree you are editing therefore freezes your editing\n    surface — a warning comes back in the register response when a file the\n    daemon has open or recently checked falls under the new root, but the\n    warning is a warning: check the response, and use UnregisterBaseRoot to get\n    the subtree back.\n  * registering a root is only observable through `minas info` / GetServerInfo\n    (`base_roots`) and through `base_diagnostics` in GetState for the files the\n    daemon maps to that root. An empty `base_diagnostics` right after\n    registering is not proof that it failed — the comparison diagnostics arrive\n    asynchronously (and only for the mapped `repo`).\n- Prefer the dedicated subcommands in scripts; exec is for commands without one.\n- `GetServerInfo` returns the DAEMON's build hash / timestamp / metrics — the\n  same information as `minas info`. Its `base_roots` field lists only comparison\n  roots registered via `RegisterBaseRoot`; an empty array is normal and does NOT\n  mean \"no language server\" — language servers are resolved per file / workspace\n  root (rust2 #27).\n- Its `servers` field answers \"which LSP servers are configured, for which\n  languages, and how many sessions are running\": `[{id, languages,\n  running_sessions}]` (rust2 ask). If `check` said \"no LSP server configured\",\n  look here: no `servers` entry covering that file's language means\n  `languages.toml` (daemon-side config) has no server mapped to it."
  },
  {
    "name": "hints",
    "description": "minas hints <path>: inlay hints (types/params) without full text",
    "body": "HINTS — inlay hints for any path (ADR-0020)\n\nUse:      minas hints <path>\nOutput:   the raw inlay-hint array as JSON (`[]` when the server emits none). No file text.\n\nRules\n- Requires a language server. A path whose language has no server entry is REFUSED with\n  `hints not supported for … `, exit 1 (before 2026-09-14 it returned `[]` exit 0 — an empty\n  answer that read like \"no hints here\"; dogfooding #7).\n- `[]` means \"the server produced no hints\", which is also what a server that computes none\n  returns — it is not evidence about the code. A positive signal is server-dependent: measured\n  with typescript-language-server, `hints` returned `[]` for a file where `hover` had real type\n  information, so prefer `hover` / `at` when you need a type and treat `hints` as decoration.\n- Read-only: no generation bump, no push. \n"
  },
  {
    "name": "peek",
    "description": "minas peek <path> <line>:<col>: definition snippet without full text",
    "body": "PEEK — definition snippet for a position (ADR-0025)\n\nUse:      minas peek <path> <line>:<col>      (1-origin; col is a char count)\nOutput:   {path, line, text} as JSON — the definition's file, its 1-origin start line, and a\n          few lines of source. Empty text = no definition found (or none advertised).\n\nRules\n- Requires a language server: a path whose language has no server entry is REFUSED with\n  `peek not supported for … `, exit 1 (before 2026-09-14 it returned `{\"text\":\"\"}` exit 0 —\n  indistinguishable from \"nothing here\"; dogfooding #7).\n- `text` empty with a live server means the server did not answer a definition at that\n  position (`definitionProvider` missing, or no definition). For cross-file navigation prefer\n  `minas symbol` / `minas references` — they say WHERE, peek says WHAT.\n- `text` is a SMALL WINDOW (a few lines), NOT the whole definition, and the response has no\n  `truncated`/`total_lines` field — a 3-line function and the first 3 lines of a 300-line one\n  look identical (measured 2026-09-15). For a whole body take the range from `outline` and use\n  `minas read --span <anchor>:<head>`.\n- Read-only, no generation bump.\n"
  }
]
